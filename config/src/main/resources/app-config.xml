<?xml version="1.0" encoding="UTF-8"?>
<application xmlns="http://appconfig.aura.nav.no" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://appconfig.aura.nav.no http://maven.adeo.no/xsd/appconfig.xsd">
    <name>modiabrukerdialog</name>
    <selftestPagePath>/modiabrukerdialog/internal/selftest</selftestPagePath>
    <artifacts>
        <ear groupId="no.nav.sbl.dialogarena" artifactId="modiabrukerdialog-jee" />
    </artifacts>
    <security serviceUserResourceAlias="srvModiabrukerdialog">
        <logins>
            <spnego />
        </logins>
    </security>
    <resources>
        <!-- Felles -->
        <webservice alias="virksomhet:Personsok_v1" mapToProperty="personsokendpoint" />
        <ldap alias="ldap" mapToProperty="ldap"/>
        <!-- Slutt felles -->
        <!-- Fra personsok -->
        <!-- TOMT -->
        <!-- Slutt personsok -->
        <!-- Fra kjerneinfo -->
        <baseUrl alias="tjenestebuss" mapToProperty="tjenestebuss"/>
        <credential alias="srv_kjerneinfo_tjenestebuss" mapToProperty="ctjenestebuss" />
        <baseUrl alias="server.gosys" mapToProperty="server.gosys"/>
        <baseUrl alias="server.arena" mapToProperty="server.arena"/>
        <webservice alias="virksomhet:Person_v1" mapToProperty="kjerneinfoendpoint"/>
        <webservice alias="virksomhet:Kodeverk_v1" mapToProperty="kodeverkendpoint"/>
        <webservice alias="virksomhet:Brukerprofil_v1" mapToProperty="brukerprofilendpoint"/>
        <webservice alias="virksomhet:BehandleBrukerprofil_v1" mapToProperty="behandlebrukerprofilendpoint"/>
        <webservice alias="virksomhet:Ytelseskontrakt_v1" mapToProperty="ytelseskontraktendpoint"/>     <!-- mangler i T8 ?-->
        <webservice alias="virksomhet:Oppfolging_v1" mapToProperty="oppfolgingendpoint"/>   <!-- mangler i T8 ?-->
        <webservice alias="virksomhet:foreldrepenger_v1" mapToProperty="foreldrepengerendpoint"/>
        <webservice alias="virksomhet:sykepenger_v1" mapToProperty="sykepengerendpoint"/>
        <webservice alias="domene.Brukerdialog:HenvendelseService_v1" mapToProperty="henvendelseendpoint"/>
        <webservice alias="domene.Brukerdialog:BesvareHenvendelseService_v1" mapToProperty="besvarehenvendelseendpoint"/>
        <webservice alias="domene.Brukerdialog:OppgavebehandlingService_v1" mapToProperty="oppgavebehandlingendpoint"/>

        <!-- Slutt kjerneinfo -->
        <!-- App-->
        <webservice alias="SakOgBehandling" mapToProperty="sakogbehandling"/>
        <webservice alias="Aktoer_v1" mapToProperty="aktor"/>

        <applicationCertificate alias="srvModiabrukerdialog" />
    </resources>
</application>