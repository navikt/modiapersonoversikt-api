query($identer: [ID!]!){
    hentPersonBolk(identer: $identer) {
        ident,
        code,
        person {
            navn {
                fornavn,
                mellomnavn,
                etternavn
            }
        }
    }
}
