query hentIdenter($ident: ID!, $grupper: [IdentGruppe!]){
    hentIdenter(ident: $ident, grupper: $grupper, historikk: true) {
        identer {
            ident
            gruppe
            historisk
        }
    }
}
